v17.11.2 최소 패치

사용자가 올린 정상 작동 v17.11.1을 그대로 기준으로 했습니다.

원본과 완전히 동일하게 유지:
index.html / admin.html / member.html / display.html / styles.css / package.json / render.yaml

수정한 파일:
app.js / server.js

수정 내용:
- 시작 누른 화면이 먼저 혼자 출발하지 않고 서버가 raceId + seed + 공통 시작시각을 먼저 확정.
- 같은 방의 모든 화면이 약 1.4초 뒤 같은 시각에 각자 로컬 물리로 동시에 시작.
- 이미 진행 중인 화면은 다른 화면 snapshot 좌표를 계속 받아 덮어쓰지 않음.
- 로컬 레이스 진행 중에는 큰 /api/state snapshot 폴링도 중단해 네트워크/JSON 렉 감소.
- 시작한 화면만 snapshot과 공식 finish/result를 서버에 전송해 중복 결과 방지.
- 기존 클릭 동기화, 방 구조, 참가자 입력, 맵, 물리엔진, 카메라, 당첨팝업, 공 개수 제한은 수정하지 않음.

목표: 기존 사이트 정상 작동 그대로 + 다중 화면 동시 시작 + '멈췄다가 따라가는 렉' 제거.
